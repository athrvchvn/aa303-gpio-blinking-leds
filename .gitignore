# LaTeX build artefacts
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Python
__pycache__/
*.pyc

# Raw camera/WhatsApp originals (renamed copies live in figures/)
WhatsApp Image *.jpeg

# Reference document from another student - not mine to publish
Basic Operations Using GPIO Blinking LEDs.pdf
first figure image.jpeg

# Raw demonstration videos for Report 3 (shared via Google Drive; stills live in report-3/figures/)
Pulse Width Modulation using GPIO /

# Raw photos/videos for Report 4 (shared via Google Drive; crops and stills live in report-4/figures/)
DHT11 sensor Data/

# Raw photos/videos for Report 5 (shared via Google Drive; crops and stills live in report-5/figures/)
BMP-280 sensor data reading for pressure, temperature and altitude/

# Raw videos for Report 6 (shared via Google Drive; stills live in the report's figures/)
IMU sensor data reading for inertial navigationaccelarationvelocity/
